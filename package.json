{
  "name": "fasal-pramaan",
  "private": true,
  "description": "Monorepo. The Next.js app is apps/dashboard. Set the Vercel Root Directory to apps/dashboard.",
  "scripts": {
    "dev": "npm run dev --prefix apps/dashboard",
    "build": "npm run build --prefix apps/dashboard",
    "start": "npm run start --prefix apps/dashboard"
  }
}

# Fasal-Pramaan Vercel environment
# The project only needs variables beyond apps/dashboard/.env.example that are
# Vercel-specific. Keep the Root Directory set to apps/dashboard.
#
# Locks the preview/production site behind a password gate (/unlock).
SITE_LOCK_PASSWORD=
